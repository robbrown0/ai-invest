# Human Checkpoint — Approve the Fixed Synthetic IO Entry Scope

**STOP before integration/installation.** This is a policy-scope approval checkpoint, not another console diagnostic and not an instruction to enter any value. Gate 2 NOT PASSED; both authorization flags false.

The [successful finite observer result and input/output criteria](INPUT_OUTPUT_QUALIFICATION.md) and [independent candidate review](INPUT_OUTPUT_REVIEW.md) are ready for review. The old /var/tmp/ai-invest-crash-traversal.json is preserved in full by the human; do not rerun or delete it.

## One decision requested

Approve preparing the narrowly scoped **additional exact --io-test argument** for the existing /usr/local/sbin/ai-invest-operator-preflight command, solely for the documented disposable synthetic display/retype exercise. This is not covered by calling a new grant a digest refresh. No installed or repository sudoers policy has been expanded in this amendment.

The planned grant must retain PASSWD, NOSETENV, fresh sudo authentication, exact digest, fixed arguments only, command-specific PTY and I/O-log exceptions, secure environment, verified local console/session/operator/host, and bounded resources. It grants no arbitrary runner, caller-selected executable/path/environment, shell, bootstrap, real secret or additional consumer. Existing --diagnostic and --crash-test keep their meanings. Ordinary sudo retains its protections.

Current state: terminal_exchange.py is an **unwired non-executable library**, no CLI. The current wrapper rejects --io-test and its candidate report schema. Do not run it with sudo Python, an interactive root shell or an old installation block. No new root artifacts, result files or recovery copies have been created.

## What happens only after that approval

Complete the fixed protected-worker integration, independently review actual descriptors/transitions and bounded cleanup/publication, and produce one current guarded installation block with exact final wrapper/helper/policy hashes, baseline checks, candidate and aggregate visudo validation, root ownership/modes/link checks, preservation of every old artifact/copy, and reversible project-only rollback. No executable digest is guessed for unimplemented integration and no host policy editing is requested now.

The intended eventual console command is **sudo /usr/local/sbin/ai-invest-operator-preflight --io-test**, but it is **not implemented or authorized to run yet**. Proposed future exclusive result: /var/tmp/ai-invest-synthetic-io.json. The eventual handoff must include checked non-symlink/root-owner/mode/link-count retrieval of that fixed sanitized result; no such artifact exists from this task.

Before any future prompt, the human must trust the dedicated physical-console session and must not run a recorder. Another process with terminal access is outside the current confidentiality claim. Enter only the tool-displayed SYNTHETIC fixture, never a real password, key, token or share. Do not transmit the fixture back to the assistant. The integrated checkpoint must make its protection/restoration outcomes and unperformed tests explicit.

No install/rollback command is supplied at this approval stop because no privileged artifact was changed. The current reviewed installed checkpoint stays intact; there is nothing to roll back from this amendment. Human approval would authorize completing the synthetic IO integration, **not** real-secret entry, LUKS/OpenBao initialization or Gate 2 passage.
