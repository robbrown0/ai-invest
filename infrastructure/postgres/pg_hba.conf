# Dedicated local administrator and two independently tested synthetic tenants.
local all postgres peer
local ai_invest ai_tenant_a peer
local ai_invest ai_tenant_b peer
local ai_invest ai_web_a peer
local ai_invest ai_lan_status peer
local all all reject
host all all 0.0.0.0/0 reject
host all all ::/0 reject
